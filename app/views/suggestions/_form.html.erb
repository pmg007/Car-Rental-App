<%= form_with(model: suggestion, local: true) do |form| %>
  <% if suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(suggestion.errors.count, "error") %> prohibited this suggestion from being saved:</h2>

      <ul>
      <% suggestion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


    <div class="field">
      <%= form.label :model %>
      <%= form.text_field :model, id: :car_model %>
    </div>

    <div class="field">
      <%= form.label :manufacturer %>
      <%= form.text_field :manufacturer, id: :car_manufacturer %>
    </div>

    <div class="field">
      <%= form.label :location %>
      <%= form.text_field :location, id: :car_location%>
    </div>

    <div class="field">
      <%= form.label :style %>
      <%= form.text_field :style, id: :car_style %>
    </div>

  <div class="field">
    <%= form.label :suggestion  %>
    <%= form.text_area :suggestion, id: :suggestion_suggestion %>
  </div>

 <!--  <div class="field">
    <%= form.label :accepted %>
    <%= form.check_box :accepted, id: :suggestion_accepted %>
  </div> -->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
